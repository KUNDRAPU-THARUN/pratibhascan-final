<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/sport_details_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_dark"
    tools:context=".AcademySportDetailsActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent">
        <include layout="@layout/layout_global_header" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/tv_sport_detail_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sport Management"
                android:textColor="@color/white"
                android:textSize="24sp"
                android:textStyle="bold"
                android:layout_marginBottom="20dp" />

            <!-- Athletes Section -->
            <include layout="@layout/layout_sport_detail_section" android:id="@+id/section_athletes" />

            <!-- Coaches Section -->
            <include layout="@layout/layout_sport_detail_section" android:id="@+id/section_coaches" />

            <!-- Grounds Section -->
            <include layout="@layout/layout_sport_detail_section" android:id="@+id/section_grounds" />

            <!-- Tournaments Section -->
            <include layout="@layout/layout_sport_detail_section" android:id="@+id/section_tournaments" />

            <!-- Registrations Section -->
            <include layout="@layout/layout_sport_detail_section" android:id="@+id/section_registrations" />

            <!-- Attendance Section -->
            <include layout="@layout/layout_sport_detail_section" android:id="@+id/section_attendance" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
